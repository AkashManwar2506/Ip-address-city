// const validateIp = (req, res, next)=>{
//     const { ip } = req.params;
//     const isValid = //.test(ip);
//     if (!isValid) return res.status(400).json({ error: 'Invalid IP address' });
//     next();
//   }

//   module.exports = validateIp